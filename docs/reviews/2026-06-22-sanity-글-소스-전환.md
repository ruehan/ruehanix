# 리뷰 기록 — 블로그 글 Sanity 소스 전환

- 날짜: 2026-06-22
- 브랜치: feat/sanity-posts-source
- 최종 판정: 통과 (1라운드, P3 반영)

## 1라운드
- 판정: 통과 (P3 권고 1건)
- 검증: 리뷰어 직접 — verify exit 0(typecheck·eslint 0 error/0 warn·vitest 44/44), build OK(/posts·[slug] SSG),
  smoke 17/17. 런타임 실측: `/posts` 빈 상태, `/posts/없는슬러그` → 404, `/` 200, sitemap 정적 경로만.
  서버→클라 직렬화(BlogPost 순수 데이터) 안전, 하이드레이션 일치 확인.
- 확인: id→slug 전환 전 경로 일관, vm.post null 가드, 빈 상태 4곳, 소스 추상화(source.ts만 교체) 동작, 하드코딩 잔존 0.
- 지적사항:
  - [P3] `lib/ruehanix/types.ts`의 `Post` 인터페이스가 미사용 죽은 코드로 잔존(export라 ESLint 미검출).
    `INITIAL.selected="p1"`도 구 id 잔재(런타임은 posts[0].slug로 덮임).
  - [관찰] 스모크가 글 콘텐츠 검증 → 글-무관 마커로 약화(빈 데이터셋이라 불가피, ADR 명시). 글 채운 뒤 복원 백로그 권고.
- 반영:
  - [P3] `Post` 인터페이스 삭제, `INITIAL.selected`를 `""`로 정리. 재검증 tsc clean·vitest 44/44.
  - [백로그] ADR 0010에 "글 렌더 스모크 복원" 백로그 추가.
  - [기록] 본 리뷰 기록 + worklog 작성.

## 비고
- 빌드/스모크의 Sanity 네트워크 의존은 실제 CMS 블로그의 본질로 수용(ADR 0010 트레이드오프).
