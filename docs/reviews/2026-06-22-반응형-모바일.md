# 리뷰 기록 — 반응형 / 모바일 모드

- 날짜: 2026-06-22
- 브랜치: feat/responsive-mobile
- 최종 판정: 통과 (1라운드, 병합 전 기록 보완 + P3 정리)

## 1라운드
- 판정: 통과 (P1/P2 결함 없음, 병합 전 기록 필수)
- 검증: 리뷰어 직접 재실행 — verify.sh 통과(typecheck·lint 무경고·vitest 20/20),
  smoke 8/8, build 4/4. 개발 직원 보고와 일치.
- 확인: 데스크톱 경로 무변경(모바일 분기 추가만), 독 onClick 루프 변수 캡처 정상,
  homeClick/mobileHome 조건 정확, 리사이즈 경계 전환 시 상태 보존, SSR 마크업 일치(초기 vp 1280).
- 지적사항(모두 비차단 P3):
  - [P3] `docs/worklog.md` 항목·`docs/reviews/` 파일 없음 → 병합 전 필수.
  - [P3] `useRuehanix.ts` onKey가 모바일에서도 Super+1~6/Q 실행(무해하나 표면적 불일치).
  - [P3] `responsive.test.ts`에 mobileAppRect 음수 높이 경계 미검증.
- 반영:
  - [P3-기록] 본 리뷰 기록 + worklog 항목 추가.
  - [P3-키보드] onKey 진입부에 `isMobileWidth(window.innerWidth)` 가드 추가 — 모바일에선 Super 단축키 무시.
  - [P3-테스트] `mobileAppRect` 높이를 `Math.max(0, …)`로 방어 + 낮은 뷰포트(H=80) 음수 방지 테스트 추가(21/21).

## 비고
- 구조(실용적 분해)는 ADR 0002/0003 기록, 재지적 없음.
- 향후 후보: 모바일 스와이프 제스처, safe-area-inset 대응.
