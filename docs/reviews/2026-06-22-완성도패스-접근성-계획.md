# 리뷰 기록 — 완성도 패스(메타·부팅·검색·접근성) + 콘텐츠/SEO 계획

- 날짜: 2026-06-22
- 브랜치: feat/polish-and-content-plan
- 최종 판정: 통과 (1라운드, P2 반영)

## 1라운드
- 판정: 통과 (P2 권고 1건, 병합 차단 아님)
- 검증: 리뷰어 직접 재실행 — verify.sh 통과(typecheck·lint 무경고·vitest 32/32),
  build 5/5(icon.svg·not-found·opengraph-image 포함), smoke 14/14. prerender HTML에
  canonical 절대화·og:*·twitter·theme-color(media)·robots 정상 확인. Playwright 실인터랙션
  (런처 Enter 열림·검색 필터·Esc 후 query 초기화·focus-visible) 확인.
- 적대적 점검(문제 없음): SSR 하이드레이션(booting 고정값), sessionStorage try/catch, reboot 항상 재생,
  cleanup bt undefined 무해, query 초기화 4경로, close 타입 확장 안전, reduced-motion이 404 깜빡임도 정지.
- 지적사항:
  - [P2] `twitter.card: summary_large_image` 선언했는데 og:image·twitter:image가 전무 →
    공유 미리보기가 빈/깨진 대형 카드. 정적 폴백 OG 필요(동적 글별 OG는 ADR 0006대로 후속이 타당).
  - 슬라이더(gap 드래그) 키보드 미지원은 시각 미세조정이라 타당(토글·모드·강조색은 모두 접근 가능) — 비차단.
- 반영:
  - [P2] `app/opengraph-image.tsx`(next/og) 추가 — 빌드타임에 ruehanix 브랜딩 OG(1200×630) 생성.
    파일 컨벤션이 og:image·twitter:image를 자동 주입. prerender HTML에서 두 메타 채워짐 확인
    (`og:image`/`twitter:image` = /opengraph-image). 한글 폰트 이슈 회피 위해 OG 텍스트는 영문.
  - [기록] 본 리뷰 기록 + worklog 항목 추가.

## 비고
- 콘텐츠/SEO 실제 구현은 ADR 0006의 점진 이전 순서대로 후속 /feature로 분리.
