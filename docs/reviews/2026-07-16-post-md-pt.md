# 리뷰 기록 — md → Portable Text 변환기 교체 (ADR 0054)

- 날짜: 2026-07-16
- 브랜치: feat/post-md-pt
- 최종 판정: 통과 (1라운드 + P0 후속 반영)

## 1라운드

- 판정: 수정 필요
- 검증: typecheck 0 / eslint 0 / vitest 281 / build 13/13 / smoke 24/24

### 지적사항 (P0/P1/P2)

- **[P0] 마크다운 이미지가 통째로 null 렌더** — `PostBody.tsx:42-48` 의 `if (!v.asset) return null` 가 `@portabletext/markdown` 의 `{src, alt}` 출력을 차단. 실제 `content/posts/*.md` 에 이미지 없어 빌드 통과했지만, 향후 md 한 줄 추가하면 사라짐. **수정: `v.src` 분기 — raw img 렌더.**
- **[P1] 커밋 메시지 / ADR 진술 부정확** — "테스트 11개" (실제 12), "PortableTextObject union 추가" (이미 정의됨).
- **[P1] 기록 누락** — `docs/worklog.md` 항목, `docs/reviews/2026-07-16-post-md-pt.md` 부재.
- **[P2] working tree 잔여물** — `content/posts/ruehanix-데스크톱을-닮은-블로그.md` unstaged, `.claude/` untracked (이전 작업 잔재로 본 PR 무관).

### 반영

- `components/posts/PostBody.tsx` — `v.asset` 와 `v.src` 두 분기. md 이미지 raw `<img src={v.src} alt={v.alt} loading="lazy" ... />` 렌더. Sanity 자원은 기존 `urlFor(...)` 경로.
- `lib/posts/markdown.test.ts` — image 케이스 강화 (`{_type, src, alt}` shape 검증).
- `docs/decisions/0054-md-to-portable-text.md` — ADR 정정 (12 케이스, PortableTextObject 정의 자체).
- `docs/worklog.md` — 항목 추가.
- 본 review 파일 작성.

## 통과

- PostBody md 경로 img 정상 렌더.
- table 처리 차기 과제.
- `---` 다음 머리말 충돌 방지 차기.
