# 리뷰 기록 — Sanity 임베드 스튜디오 (+ 강제 Next 16 업그레이드)

- 날짜: 2026-06-22
- 브랜치: feat/sanity-embedded-studio
- 최종 판정: 통과 (1라운드, P2/P3 반영)

## 1라운드
- 판정: 통과 (조건부 — 기록 작성 + P2/P3 권고)
- 검증: 리뷰어 직접 재실행 — verify exit 0(typecheck OK, lint 0 error / 12 warn=완화된 신규 규칙,
  vitest 37/37), 클린 빌드 OK(Next 16.2.9, /studio=ƒ), smoke 14/14(앱 회귀 없음).
- 시크릿 안전(정밀): `.env.local` gitignore+미추적, 실제 토큰값으로 `.next`/클라이언트 번들 스캔 →
  어디에도 없음. `SANITY_API_READ_TOKEN`은 NEXT_PUBLIC_ 아님(노출 경로 없음). projectId/dataset만 공개.
- 지적사항:
  - [P2] `readToken` export만 되고 미사용 — 비공개 조회 미배선(공개 데이터셋이면 무해).
  - [P2] `formatDate`가 로컬 타임존 의존 → 발행일 하루 어긋남/hydration mismatch 위험. 테스트가
    정규식으로 이 민감도를 회피(검증 못 함).
  - [P3] tsconfig `jsx: preserve→react-jsx` 변경 ADR 미기록.
  - [P3] `.gitignore`에 `!.env.example` 예외 넣었으나 `.env.example` 파일 없음.
  - [P3] 스키마 `cover` 필드 normalize/types/queries에 미매핑.
- 반영:
  - [P2-date] `formatDate`를 UTC 기준(getUTC*)으로 고정. 테스트를 정확 비교(`2026.06.18`)+늦은 UTC
    경계 케이스로 강화(38개, 민감도 실검증).
  - [P2-token] env.ts에 readToken 미사용 사유·후속 배선 백로그 주석 명시.
  - [P3-tsconfig] ADR 0007에 jsx 변경·styled-components peer 기록 추가.
  - [P3-example] `.env.example`(키만, 값 없이) 추가.
  - [P3-cover] ADR 0007 백로그에 cover 미매핑·readToken 배선·useRuehanix 리팩터링 명시.
  - [기록] 본 리뷰 기록 + worklog 항목 작성.

## 비고
- 13 콘솔 에러(=/studio 시각 확인)는 전부 CORS(테스트 포트 미등록). 스튜디오 mount·프로젝트 접속은 정상.
- lint warn 12종 = 강제 Next 16의 react-hooks v6 신규 규칙 완화분. useRuehanix 리팩터링 백로그.
