# 리뷰 기록 — UI 설정 영속화 + 빈 상태 문구 정리

- 날짜: 2026-06-22
- 브랜치: feat/persist-ui-settings
- 최종 판정: 통과 (1라운드)

## 1라운드
- 판정: 통과
- 검증: 리뷰어 직접 — verify exit 0(typecheck·eslint 0 error/0 warn·vitest 49/49), build OK,
  smoke 18/18(UI 재접속 복원 시나리오 프로덕션 빌드에서 PASS 확인).
- 정밀 확인:
  - 복원 effect가 저장 effect보다 먼저 선언 → 마운트 시 저장 effect 첫 실행은 uiSavedRef 가드로 스킵
    (기본값 덮어쓰기 방지). 복원 후 재실행은 복원값 재저장(idempotent). savedUi null이면 저장 안 함.
  - 단일 setState 병합(savedUi/skipBoot 조합) 정확. parseUiState gap 범위(0~28)가 setGap 출력과 일치.
  - set-state-in-effect disable **정확히 1줄**(grep 카운트 main=1, 현재=1, 늘지 않음).
  - 하이드레이션 mismatch 없음(초기 INITIAL 고정, 복원 마운트 후). React Compiler 0 warn 유지.
- 지적사항:
  - [P3] dev StrictMode 이중 마운트 시 2차 마운트에서 저장 effect가 한 번 돌 수 있음 — 복원값이라
    idempotent·무해, 프로덕션(next start) 미발생. 수정 불요, 인지만.
- 반영: P3는 수정 불요(prod 무영향). 본 리뷰 기록 + worklog 작성.

## 비고
- 재방문 theme flash 완전 제거는 ADR 0011 백로그(layout head 인라인 스크립트).
