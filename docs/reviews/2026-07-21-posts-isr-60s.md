# 리뷰 기록 — /posts·/posts/[slug] SSG → ISR 60s
- 날짜: 2026-07-21
- 브랜치: feat/posts-isr-60s
- 최종 판정: 통과 (1라운드, self-review)

## 1라운드
- 판정: 통과
- 검증: typecheck 0 / eslint 0 error (2 warning 기존 — CodeBlockClient.tsx·PostBody.tsx, 본 변경 무관) / vitest 42 files / 313 tests / build 10/10 + /studio dynamic. 라우트 표에서 `/posts` `○ (Static) Revalidate 1m Expire 1y`, `/posts/[slug]` `● (SSG) Revalidate 1m Expire 1y` — 둘 다 revalidate 컬럼이 1m 로 표시되어 ISR 전환이 빌드 산출물에 정확히 반영됨을 확인. 기존 prerender 1건(`/posts/why-a-desktop-shaped-blog`) 도 1m revalidate 가 붙어 있음.
- 지적사항: 없음
- 반영: —
- 비고: 변경 파일이 정확히 2개(`app/posts/page.tsx`, `app/posts/[slug]/page.tsx`)에 각 1줄(`export const revalidate = 60;`)만 추가. 다른 페이지/컴포넌트/데이터 fetch 로직은 손대지 않음. 페이지 단위 회귀 테스트는 기존 컨벤션(컴포넌트/라이브러리 단위 위주, e2e/smoke 로 통합 검증) 에 따라 추가하지 않음 — 영향 표면이 1줄짜리 캐시 전략 상수이고 빌드 산출물 라우트 표 + build 성공으로 정확히 검증됨.
