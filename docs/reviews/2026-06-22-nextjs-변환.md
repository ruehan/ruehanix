# 리뷰 기록 — RueHanix Next.js 변환

- 날짜: 2026-06-22
- 브랜치: feat/nextjs-conversion
- 최종 판정: 통과 (1라운드, 병합 전 기록 보완)

## 1라운드
- 판정: 통과 (조건부 — 병합 전 기록 2건 필수)
- 검증: 리뷰어가 직접 재실행 — `verify.sh` 통과(typecheck OK, lint 무경고, vitest 16/16),
  `npm run build` 정적 프리렌더 4/4, `npm run smoke` 6/6. 개발 직원 보고와 일치(과장 없음).
- 충실도: 원본 index.html 905행 전체 대조 — 8개 앱·waybar·위젯·런처·키바인드·부트·타일링·테마 엔진
  모두 동작 동치 확인. 훅 변환의 TDZ/stale closure/리스너 누수 위험 없음(useCallback 안정 참조 +
  stRef.current 최신값 읽기 + cleanup 5리스너·2인터벌 정리). SSR/하이드레이션 안전(초기 상태 고정값).
- 지적사항:
  - [P1] `docs/worklog.md`에 이번 변환 항목 없음, `docs/reviews/`에 이번 리뷰 기록 없음 → 병합 전 필수.
  - [P3] `components/ruehanix/apps.tsx:5` `sans` 상수 미사용(데드코드).
  - [P3] `components/ruehanix/useRuehanix.ts:79-81` applyTheme effect deps에 `st` 전체 중복(무해, idempotent).
  - [P3] smoke에 키보드 단축키·드래그 거터 시나리오 빠짐(시각 확인으로 보완됨).
- 반영:
  - [P1] 본 리뷰 기록 + `docs/worklog.md` 변환 항목 추가.
  - [P3-sans] 미사용 상수 제거.
  - [P3-deps] effect를 `applyTheme(stRef.current)` + deps `[st.ui.mode, st.ui.accent, applyTheme]`로 축소.
  - [P3-smoke] 키보드·드래그 시나리오는 비차단 백로그로 남김(worklog/ADR 명시). 재검증 시 verify 통과 유지.

## 비고
- 구조(실용적 분해 vs FSD)는 사용자가 명시 선택, ADR 0002로 예외 기록 → 리뷰어가 비차단 처리.
