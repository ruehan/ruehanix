# 리뷰 기록 — 팝오버 아이콘 수정 + 플레이리스트 Sanity 전환

- 날짜: 2026-06-23
- 브랜치: feat/sanity-tracks
- 최종 판정: 통과 (2라운드)

## 1라운드
- 판정: 수정 필요
- 검증: verify 통과(typecheck 0·eslint 0/0·vitest 72/72), build 성공, smoke 19/19(현재 Sanity 트랙 0개 → 미니플레이어 숨김 경로만 실행).
- 지적사항:
  - [P1] viewModel.ts/useRuehanix.ts — 저장 index가 곡 수 범위를 벗어날 때 표시 곡(viewModel `min(index, n-1)` 클램프)과 reducer 계산(상태 원본 index에 modulo)이 어긋남. 곡 수가 줄면(예: 5곡→3곡, 저장 index=4) 첫 next/prev/onEnded가 잘못된 곡으로 가거나 제자리. 마운트 effect의 클램프를 표시 시점으로만 옮긴 결정이 낳은 회귀.
  - [P2] (권장) 트랙 0개라 팝오버·아이콘·클램프가 smoke 사각지대. 핵심 로직을 순수 단위 테스트로 못박을 것.
  - (회귀 없음 확인) 아이콘 fix `flex:none` 정확, Sanity 전환은 posts 패턴과 일관.
- 반영:
  - P1 — player.ts에 `clampIdx(i,n)=min(max(0,i),n-1)` 추가, `playerNext`·`playerPrev`·`onEnded` 진입 시 index 정규화 → reducer가 보는 index와 viewModel 표시 index를 동일 기준으로 일치. onEnded의 one/off 분기도 정규화된 index 반환(상태 self-heal).
  - P2 — 범위 밖 index 단위 테스트 6개 추가(playerNext 4/3·10/3, playerPrev 4/3, onEnded all/off/one 4/3).

## 2라운드
- 판정: 통과
- 검증: verify 통과(typecheck 0·eslint 0 error/0 warn·vitest 78/78 — player 21개), build 성공, smoke 19/19 2회.
- 신규 결함: 없음. 변경이 player.ts reducer 3함수+테스트로 국한, 기존 정상범위 테스트 15개 그대로 통과(회귀 없음). clampIdx 결과가 항상 유효 범위라 엔진·표시 일관.
