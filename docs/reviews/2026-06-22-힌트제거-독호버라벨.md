# 리뷰 기록 — 첫 방문 힌트 제거 · 독 호버 이름 라벨

- 날짜: 2026-06-22
- 브랜치: feat/dock-tooltip-remove-hint
- 최종 판정: 통과 (2라운드)

## 1라운드
- 판정: 수정 필요
- 검증: 리뷰어 직접 재실행 — typecheck OK, lint 무경고, vitest 22/22, build 4/4, smoke 11/11.
- 지적사항:
  - [P1] 데스크톱 독 호버 라벨이 opacity 1이지만 화면에 안 보임. 원인: 독 컨테이너 `overflowX:auto`가
    위로 튀어나온 라벨을 클리핑. smoke가 opacity만 검사해 결함을 못 잡음.
  - [P2] worklog·review 기록 누락(병합 전 작성 예정 — 절차상 정상).
  - [P3] 라벨 텍스트색 d.color(파스텔)이 라이트 모드 밝은 배경에서 대비 부족 가능.
- 반영:
  - [P1] 독 컨테이너 `overflowX:auto`·`maxWidth` 제거(8앱이 항상 화면에 들어가 불필요 → 클리핑 해소).
    `.rh-dock-label`에 `z-index:10` 추가. backdrop-filter 블러는 유지.
  - [P1-센서] smoke 호버 시나리오를 강화 — 라벨에 임시 pointer-events:auto 부여 후
    `elementFromPoint(라벨 중심)`이 라벨 자신인지(클리핑·가림 없음) + `r.top>=0` 검사.
  - [P3] 라벨 텍스트색을 인라인 d.color → CSS `color: var(--text)`로(다크/라이트 양쪽 대비 보장).

## 2라운드
- 판정: 통과
- 검증: 리뷰어 직접 — verify.sh 통과(typecheck·lint 무경고·vitest 22/22), build OK, smoke 11/11.
- 센서 검출력 직접 증명: 1라운드 결함(overflowX:auto+maxWidth)을 일시 복원해 돌리니 "독 호버 이름 라벨"이
  FAIL(10/11), 정상 상태에서 PASS(11/11) — 강화 센서가 결함을 실제로 잡음 확인.
- P1 실해결(클리핑 해소, 라벨이 윈도우 레이어 위로 보임), P3 양쪽 모드 대비 적절(Catppuccin AA),
  768px 회귀 안전(독 폭 ≈380px), 신규 버그 없음.
- 신규 결함: 없음.
