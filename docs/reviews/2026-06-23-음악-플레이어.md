# 리뷰 기록 — 음악 플레이어 (rhx-play)

- 날짜: 2026-06-23
- 브랜치: feat/music-player
- 최종 판정: 통과 (2라운드)

## 1라운드
- 판정: 수정 필요
- 검증: verify 통과(typecheck 0·eslint 0/0·vitest 68/68), build 성공. 단 smoke가 **간헐 실패**(1회차 11/12, 2회차 21/21) — 작성자의 "21/21" 주장이 비결정적임을 리뷰어가 포착.
- 지적사항:
  - [P1] RuehanixShell.tsx:202 — 메뉴바 중앙 focus-title `<div>`에 `pointerEvents:none`·폭 상한 부재. 미니플레이어 추가로 우측 클러스터가 넓어지자 중앙 타이틀 박스가 미니플레이어 영역과 겹쳐 포인터 이벤트를 가로챔 → smoke `mini-playpause` 재클릭이 `<div> intercepts pointer events`로 간헐 실패. 이번 기능으로 새로 생긴 회귀.
  - [P2] smoke 보고 부정확 — flaky라 "21/21" 신뢰 불가. P1 수정 후 반복 실행으로 안정성 입증 필요.
  - [P3] player.ts toggle 빈목록 가드 누락 — 다른 reducer(next/prev/select/onEnded)는 `n<=0` 가드가 있으나 toggle만 없음. 현재 TRACKS 비어있지 않아 도달 불가하나 일관성·방어 차원 권장.
  - [P4] (관찰, 수정 불요) YouTubeEngine loadApi의 전역 `onYouTubeIframeAPIReady` 콜백이 cleanup에서 해제되지 않음. API 1회 로드 후 즉시 resolve 분기라 실사용 누수 없음. 셸 단일 인스턴스 전제에서 무해.
- 반영:
  - P1 — focus-title 컨테이너에 `pointerEvents:"none"` + `maxWidth:240` + `overflow:hidden` + `whiteSpace:nowrap` 추가, 타이틀 텍스트는 ellipsis span으로 분리. 클릭 대상이 아니므로 포인터 비활성이 정확한 수정.
  - P2 — P1 수정 후 smoke 3회 연속 21/21 통과로 비결정성 해소 입증.
  - P3 — `playerToggle`을 `TRACK_COUNT > 0 ? toggle(s.player) : s.player`로 가드.
  - P4 — 관찰 사항이라 변경하지 않음.

## 2라운드
- 판정: 통과
- 검증: verify 통과(typecheck 0·eslint 0 error/0 warn·vitest 68/68), build 성공, smoke 3회 연속 21/21.
- 신규 결함: 없음
