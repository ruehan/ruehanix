# 리뷰 기록 — 잔여 4개 작업 묶음 (3·4·5·6)

- 날짜: 2026-07-14
- 작업 4종 묶음:
  - 3번 styled-components deps 제거
  - 4번 TerminalApp·AboutApp 스모크 테스트
  - 5번 README 공개 + sync-posts 자동화
  - 6번 FotoApp next/image + YouTubeEngine dynamic import
- 최종 판정: 통과 (1 통합 라운드, 본 묶음 내에서 즉시 보강)

## 1라운드 (통합)

- 검증: typecheck 0 / eslint 0 / vitest 28 files / 214 tests / build 11/11 / smoke 24/24.

### 라운드 3 — 통과
- deps 제거 영향 명확. 코드 사용처 0 확인.

### 라운드 4 — 수정 필요 (사소)
- [P3] `cleanup()` 호출이 4 테스트 파일에 흩어짐 — vitest setupFile 추출 권장.

### 라운드 5 — 수정 필요 (P1)
- [P1] worklog/reviews 기록 부재 (CONVENTIONS.md 위반). follow-up 1커밋으로 정리.
- [P1] frontmatter 파서 중복 — `lib/posts/frontmatter.ts` 정본 + `scripts/sync-posts.mjs` 인라인.
  지금은 일치, 그러나 drift 메커니즘 없음. 옵션 1(`.ts` 직접 import via tsx), 옵션 2
  (gray-matter), 옵션 3 (sync-posts 자체 진단 모드). **이번 라운드에선 미반영**, 후속 작업.
- [P3] NDJSON git 추적 정책 README 미명시.

### 라운드 6 — 통과 (사소)
- [P3] ADR 0031 "음악 미사용 세션 비용 0" 문구 정확화 — 트랙 0개일 때만 진실.

## 반영 (본 묶음)

- 라운드 4 P3: `vitest.setup.ts` 신설 + `vitest.config.ts` 에 `setupFiles` 추가.
  4 테스트 파일에서 `cleanup` import + `afterEach(cleanup)` 블록 제거.
- 라운드 5 P3: README 에 NDJSON git 추적 의도 1줄 추가.
- 라운드 6 P3: ADR 0031 "비용 0" 문구를 "트랙 0개일 때 비용 0" 으로 좁힘.
- 라운드 5 P1: worklog 4 항목 추가 (본 파일과 동시), 본 리뷰 파일 생성.

## 미반영 (후속 작업)

- 라운드 5 P1 파서 drift — 옵션 3 (sync-posts 자체 진단 모드) 가 가장 침습 적음.
  CI 에 끼워넣어 두 구현 결과가 달라지면 즉시 실패하도록 만들 수 있다.
  비용 평가 후 별도 작업.
- 라운드 4 setupFile 보강 — 본 라운드에서 처리 완료.

## 부가

- setupFile 도입으로 4 테스트 파일이 RTL cleanup 에 의존하지 않게 됨. 향후 신규 테스트
  파일은 setupFiles 만 알면 된다.