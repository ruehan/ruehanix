# 리뷰 기록 — 음악 컨트롤러 팝오버 + 메뉴바 칩 제거

- 날짜: 2026-06-23
- 브랜치: feat/music-popover
- 최종 판정: 통과 (1라운드)

## 1라운드
- 판정: 통과
- 검증: verify 통과(typecheck 0·eslint 0 error/0 warn·vitest 68/68), build 성공, smoke 23/23(신규 팝오버 3시나리오 포함). 리뷰어 누적 24회 smoke 중 1회 "콘솔 앱 에러 0" 실패는 재현 불가(콘솔 프로브 5회 0건) — P4 참조.
- 신규 결함: 없음
  - zIndex 역전(팝오버 8000 < 런처 9000 < 단축키 9100)은 셋이 상호배타라 무해.
  - 팝오버 "재생/일시정지" 버튼 vs 플레이리스트 "...재생" 항목 aria-label은 smoke가 exact+팝오버 스코프로 구분.
  - BarChip 완전 제거, ReactNode import는 Win이 계속 사용해 유효. 시각 회귀 없음.
- 지적사항:
  - [P3] viewModel.ts mod.vol — 볼륨 칩 제거로 사용처가 사라진 죽은 데이터. 동작 영향 없으나 정리 권장.
  - [P4] (관찰, 기존 한계) smoke 콘솔 에러 게이트가 YouTube 임베드의 비결정적 콘솔 에러에 완전 견고하지 않음. 텍스트 기반 필터를 m.location() URL 기반으로 보강 권장(백로그, 블로커 아님). ADR 0012의 알려진 한계 범주.
- 반영:
  - P3 — `mod.vol: "64"` 제거. verify 재통과(68/68). 별도 커밋(refactor).
  - P4 — 관찰/기존 한계라 이번 작업에서 변경하지 않음. URL 기반 콘솔 필터 보강을 백로그에 등록.
