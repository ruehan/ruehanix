# 리뷰 기록 — ruehanix 데스크톱 셸 구현

- 날짜: 2026-06-22
- 브랜치: feat/ruehanix-desktop
- 최종 판정: 통과 (2라운드)

## 1라운드
- 판정: 수정 필요
- 검증: `verify.sh` → HARNESS GAP(종료코드 2, package.json/pyproject 없음). ADR 0001로 사유 문서화됨.
  리뷰어가 `S()` 파서를 Node로 격리 실행 + JSX key/로직 정합성 정적 검증.
- 지적사항:
  - [P1] `docs/worklog.md`, `docs/reviews/` 부재 — CLAUDE.md 워크플로 8단계 미이행.
  - [P2] 스모크 검증이 일회성·휘발성이라 독립 재현 불가. .gitignore가 `.playwright-mcp/`·`/*.png`을 빼서 검증 흔적도 없음.
  - [P3] `index.html` `S()` 파서: 값 내 세미콜론/콜론, CSS 커스텀 프로퍼티(`--x`), `-ms-` 프리픽스에서 깨짐. 현재 입력엔 해당 케이스 없어 실버그는 아님 — 방어 권장.
  - [P3] `auto` 테마 모드에 `matchMedia('change')` 라이브 리스너 없음 → OS 다크/라이트 전환 즉시 반영 안 됨.
  - [P3] 클릭 요소가 전부 `<div onClick>` — `role`/`tabIndex`/키보드 핸들러 없음(접근성).
- 반영:
  - [P1] 본 리뷰 기록 파일 + `docs/worklog.md` 생성, worklog에서 이 파일 링크.
  - [P2] `scripts/smoke.mjs`(Playwright 스모크, 6개 시나리오 + 콘솔 에러 검사) 커밋. `npm run smoke`로 재현.
    실제 실행해 6/6 통과 확인. `package.json`에 playwright devDep + smoke 스크립트 선언.
  - [P3-S()] `index.html` `S()`에 제약 주석 추가 + 커스텀 프로퍼티(`--x` 원형 유지)·`-ms-`(→`ms`) 처리 보강.
  - [P3-auto] `componentDidMount`에 `matchMedia('(prefers-color-scheme: light)')` change 리스너 추가, `componentWillUnmount`에서 해제. auto 모드일 때만 `applyTheme()` 재호출.
  - [P3-접근성] 프로토타입 범위로 이번엔 미적용. React/Next 전환 백로그로 ADR 0001 "영향"과 worklog에 명시.

## 2라운드
- 판정: 통과
- 검증: `npm run smoke` → 6/6 통과 (boot 완료 · waybar · ws2 타일링 · 런처 앱 오픈 · Light 테마 전환 · 콘솔 앱 에러 0).
- 신규 결함: 없음
- 권고(비차단) 반영: smoke.mjs의 `nth()` 텍스트 셀렉터가 마크업 순서에 강결합 → 런처 버튼/워크스페이스 칩에
  `data-testid` 부여하고 `getByTestId`로 교체. 재실행 6/6 유지.
- 잔여(백로그, 비차단): 접근성(키보드/스크린리더), Babel 런타임 트랜스파일 → React/Next 전환 시 해소.
