# 리뷰 기록 — 글 라우트(/posts) + SEO

- 날짜: 2026-06-22
- 브랜치: feat/posts-routes-seo
- 최종 판정: 통과 (1라운드, P3 반영)

## 1라운드
- 판정: 통과 (P3 2건, 비차단)
- 검증: 리뷰어 직접 — verify EXIT 0(typecheck OK, lint 0 error/12 warn=기존 완화분, vitest 47/47),
  build 성공(/posts static·/posts/[slug] SSG 8글·sitemap·robots·feed). 생성 HTML 직접 검사로
  canonical 절대화·og:article·article:published_time·JSON-LD BlogPosting·title template 확인.
  sitemap 8글 slug+lastmod+priority, robots /studio 차단+sitemap, RSS 확인. POSTS id↔SLUGS 전수 일치.
- 지적사항:
  - [P3] `slugForId`를 viewModel이 source.ts가 아닌 hardcoded.ts에서 직접 import — 단일 진입점 우회.
    Sanity 교체 시 셸 매핑이 자동으로 안 따라옴.
  - [P3] RSS `pubDate`가 빈 publishedAt에서 "Invalid Date" 출력 위험(하드코딩은 무해, Sanity 교체 시).
- 반영:
  - [P3-source] `slugForId`를 source.ts에서 re-export, viewModel이 source 경유로 import. ADR 0008 명시.
  - [P3-rss] buildRssXml에서 빈/유효하지 않은 publishedAt이면 pubDate 생략(Invalid Date 미출력) + 테스트.
  - [기록] 본 리뷰 기록 + worklog 작성.
- 재검증: vitest 48/48, verify EXIT 0.

## 비고
- react-hooks v6 warn 12종은 ADR 0007의 기존 완화분(이번 무관).
